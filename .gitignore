# Compiled binaries / build output
/log-sidecar
/out/
# Stray local build binary (module-named, produced by `go build`)
/odoo-herd-log-sidecar

# Go test / coverage / profiling artifacts
*.out
*.test
*.prof
coverage.txt

# Go workspace file (machine-local)
go.work
go.work.sum

# Editor / OS cruft
*.swp
*.swo
.DS_Store
